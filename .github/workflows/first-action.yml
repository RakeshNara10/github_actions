name: First Workflow
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello! World"
      - name: Print bye
        run: echo "Good Bye"
      - name: print multi lines
        run: |
          echo "hi"
          echo "bye"

